import {StyleSheet} from 'react-native';

export const buttonStyles = StyleSheet.create({
  fab: {
    borderRadius: 50,
    justifyContent: 'center',
    alignItems: 'center',
    zIndex: 10,
    position: 'absolute',
  },
});
